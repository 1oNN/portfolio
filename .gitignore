# Dependencies
node_modules/
.pnp
.pnp.js
# package-lock.json is deliberately NOT ignored: it is tracked, and npm ci in
# CI and amplify.yml both require it. Competing lockfiles stay ignored.
yarn.lock
pnpm-lock.yaml

# Next.js build output
.next/
out/

# Production build
build/
dist/

# Environment files - NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
*.log

# OS
.DS_Store
Thumbs.db
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace
*.sublime-project
.eslintcache
.stylelintcache

# TypeScript
*.tsbuildinfo
*.tsconfig.tsbuildinfo
dist/

# Deployment / Build tools
.vercel/
.netlify/
.aws-sam/
samconfig.toml
amplify/

# Local development
data/
.gstack/

# Personal documents - NEVER commit or serve these (certificates, transcripts,
# theses, reference letters). Anything needed by the site gets copied out
# deliberately, one file at a time.
private/
